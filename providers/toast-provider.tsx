"use client";
import { Toaster } from "react-hot-toast";

// Se utiliza para dar alertas de manera global y agradable en la aplicacion

const ToasterProvider = () => {
  return <Toaster />;
};

export default ToasterProvider;
